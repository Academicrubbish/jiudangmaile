# 代码检查结果

日期：2026-09-17。版本：0.2.0。

- 业务规则、前端订阅流程与云数据库适配测试：106 项通过。
- JavaScript 语法、JSON、页面路径及云函数本地依赖路径检查：通过。
- 微信小程序生产构建：本轮前端修改后通过。
- H5 生产构建：本轮前端修改后通过。
- 编译器：uni-app Vue 3 5.26（包版本 3.0.0-alpha-5020620260914001）；开发依赖已固定版本并提供 package-lock.json。

本轮新增 6 项主动商品库回归：完整商品库可选、主动金额独立于推荐范围、临时商品及重试幂等、自定义单人请客双方快照、非法请求不覆盖原事件、停用商品服务端校验。原有剧情比例测试同步验证主动选择不改变推荐。已检查微信编译产物：独立遮罩绑定关闭、抽屉内容使用 catchtap 阻止冒泡；真机手感由用户验证。

上一轮新增 6 项剧情风格回归：四处文案均拦截空气商品叙事、生成失败回落正常备用故事、旧商品快照不污染模型输入、未接受个人事件原地修复、历史小票与请客快照保持不变，以及首页已生成事件的读取不绕过修复。

上一轮新增 12 项检查：有剩余授权的用户长期未打开仍可安排和提醒、旧接口缺少预算字段及空数据兼容、旧订阅协议提示更新、同日多事件发送、旧发送记录迁移、首个计划的时段保底、首件迟到补齐、前台暂缓补发、凭据恢复补发以及已读／暂停后停止补发。小程序与 H5 纯编译均通过。

上一轮新增 16 项订阅测试：多次授权累计、同请求重试与意图重放、并发授权、过期意图、暂停后迟到回调、设置撤销和过时设置、旧布尔许可兼容、模板及账号隔离、发送只扣一次、超时不重发、43101 校正保留并发新授权、跨天使用额度、记住选择的精确判断、普通操作同步触发、授权响应丢失恢复、暂停断网恢复及未知结果不计数。前端未绑定页面容器，进入页面和设置查询不申请授权。

上一轮新增：剧情口味三档、跨天比例、预生成取消不记比例、每日预算边界、主动消费不占比例、旧设置兼容、真实式并发只推进一次比例、普通商品的现实叙事、量词“一块桌牌”不误判价格。

上一轮新增：动态剧情梗概生成与保存、梗概长度和安全校验、备用故事及旧事件兼容、新旧模板授权隔离，以及模板 1646 四字段映射、事件标题、分转元精度、字段长度和控制字符、异常配置关闭订阅，以及发送前金额重读与无效金额不消耗许可。

上一轮新增检查：每日自动预算与主动消费分离、自定义习惯与单次范围、未读／已读状态、入站补齐到期事件、已读后停止外部提醒、多份独立请客、真实式事务冲突下只扣一次自动预算，以及旧数据迁移。

此前检查：AI 结构与金额校验、独立语义复核拒绝、超时及异常兜底、迟到结果固化、真实式云对象覆盖更新、整条场景准备与展示流程、定时重跑、设置不重置次数、通知授权意图、并发发送、暂停、夜间及未知发送结果。

原有覆盖重点：整数分金额校验、自动事件总额上限、同一操作重放、重复转存确认、跨天撤销、好友只能领取一份、自领拒绝、过期和撤销、私有事件访问、事务对象返回值及写冲突重试。

106 项自动测试使用内存存储、微信接口替身和带版本冲突的云数据库 SDK 替身，不访问真实账号、智谱 API 或真实云数据库，不发送微信消息。另按用户要求，使用实际智谱模型进行 30 条候选风格样例验证，结果独立保存于 experiments/abstract-scenes，不能与自动测试通过混为一谈。本轮小程序与 H5 均完成纯编译。结果证明本地规则与接口处理通过这些用例，不代表真实云空间部署或双账号功能联调已完成。

未启动 HBuilderX、微信开发者工具、浏览器、预览服务或小程序；未进行界面观感与真机功能验证，按用户要求由用户操作验证。

HBuilderX 命令行工具曾在当前执行环境报告处理器兼容错误，因此改用独立 uni-app 编译器完成纯编译。没有修改 HBuilderX 安装。
